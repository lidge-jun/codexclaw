#!/usr/bin/env node
// codexclaw build: compile each component's src -> dist, then aggregate
// skills/hooks/agents into the plugin root so it installs as one plugin.
// MVP stub — real build wiring lands in the build step of the MVP plan.
console.log("[codexclaw] build stub — components -> dist aggregation TBD");
