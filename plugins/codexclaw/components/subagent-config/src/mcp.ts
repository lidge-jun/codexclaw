#!/usr/bin/env node
/**
 * subagent-config — MCP server + config store.
 *
 * Responsibility:
 *  - Persist per-role subagent config: default-model vs multi-model mapping,
 *    and per-role prompt overrides.
 *  - Expose the config to the codexclaw GUI and (optionally) as MCP tools.
 *  - When ocx is present, the available model catalog is sourced from it.
 *
 * Config store: .codexclaw/subagents.json
 *
 * MVP stub: real config schema + MCP wiring lands in MVP plan step 04.
 */
process.exit(0);
