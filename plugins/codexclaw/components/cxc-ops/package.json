{
  "name": "@codexclaw/cxc-ops",
  "version": "0.2.11",
  "private": true,
  "type": "module",
  "description": "codexclaw ops CLI — doctor (plugin health), reset (scoped state cleanup).",
  "scripts": {
    "test": "node --test"
  }
}
