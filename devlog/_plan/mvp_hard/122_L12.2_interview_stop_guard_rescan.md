# L12.2 / 122 — I-phase Stop Guard + Rescan Coordinator (runtime impl)

Status: P (plan) · 2026-06-30 · mvp_hard loop L12.2 · class C3 (interview runtime)

> A-GATE HOLD (2026-06-30): an adversarial review (sub-agent Gibbs) found two hard blockers
> in the I-phase Stop-guard design below. Implementation is PAUSED pending a design decision;
> the rescan-coordinator half is unaffected. See "A-gate findings" before implementing.

> Closes the last two deferred L12 runtime items (120 "Out of L12" + 121 "Out of scope"):
> a narrow I-phase Stop guard and the contradiction-rescan coordinator helper. L12.1 (121)
> already shipped the PostToolUse answer-capture ledger this builds on.

## Gap

- `handleStop` only blocks under an ACTIVE goal mid-cycle (hook.ts guard 2b). In an
  interactive Interview there is no active goal, so a pending interview question or an open
  high-severity contradiction does NOT keep the turn alive — the session can end with the
  interview unfinished.
- `minds.ts` (selectMinds) and `triage.ts` (triageContradiction) are pure helpers with NO
  runtime caller wiring the dispatch→triage→ask→record→rescan loop together.

## Design (diff-level)

### 1. Narrow I-phase Stop guard (hook.ts)

Insert an I-phase branch in `handleStop` AFTER guard 1 (`stop_hook_active`) and the
`orchestrationActive`/`phase` read, but it does NOT require an active goal:

- Only when `state.phase === "I"`.
- Block ONLY when there is concrete pending work: a pending interview question (a
  `question_asked` with no matching `answer_recorded` in the ledger) OR an open
  high-severity contradiction (`state.interview.contradictions[].severity === "high"`).
- Reuse the SAME `MAX_STOP_BLOCKS` stagnation cap so it can never trap a session: after the
  cap at phase `I` the loop releases even if the question is still unanswered.
- Fail-safe: any read error → release (return "").
- Goal-mode reconciliation: when a goal is active, `request_user_input` is hard-denied
  upstream (PreToolUse), so the interview is suppressed and no `question_asked` is recorded;
  the I-phase guard therefore has no pending question to block on. The goal-active path keeps
  using the existing guard 2b. The I-phase branch is for the interactive (no active goal)
  interview only.

Ordering (critical): guard 1 (`stop_hook_active`) stays first so a continuation re-entry
always releases and never recurses; the I-phase branch sits after it.

### 2. rescan-coordinator.ts (pure helper)

- `hasPendingInterviewWork(cwd, sessionId, tracker)` → `{ pendingQuestionIds: string[];
  highContradictionCount: number; pending: boolean }`. Pending question = a `question_asked`
  event whose `questionId` has no later `answer_recorded` in the same ledger.
- `computeNextScanRound(tracker)` → next scan `roundId` from `tracker.scanRounds`/`roundId`.
- Documents the main-session sequence: select Minds (adaptive, cap 3) → dispatch read-only
  contradiction lenses → triage severity (high → ask_user; low/med → recorded assumption) →
  record answer (PostToolUse ledger) → rescan. The helper computes signals only; it never
  asks, writes state, or dispatches — the main session owns that (T4/T7).

## Acceptance

| Check | Evidence |
|-------|----------|
| Pending question blocks at I | question_asked w/o answer → handleStop returns block |
| Answered question releases | all questions answered → release ("") |
| High contradiction blocks at I | tracker has severity:"high" → block |
| Stagnation cap terminates | > MAX_STOP_BLOCKS at I → release even if pending |
| Non-I phases unchanged | P/A/B/C keep the active-goal-gated behavior |
| Goal-active path unchanged | active goal still uses guard 2b |
| Pure helper totals | malformed ledger → no throw, conservative pending |

## Verification

- `node --test .../test/rescan-coordinator.test.ts` + extended hook-continuation tests.
- `npm run build` idempotent; `npm test` full suite green; `git diff --check` clean.

## Out of scope

- No automatic Mind dispatch from the hook (T4: the main session owns dispatch; the hook
  only injects directives and computes signals).
- No change to the goal-active hard-deny boundary.

## A-gate findings (adversarial review — must resolve before B)

B1 (hard blocker) — pending-question match key. The ledger event identity is
`(turnId, questionId, kind)`. If a later round reuses the same `questionId` (model picks
dimension-based ids like `goal`/`scope`), an old `answer_recorded` makes a NEW
`question_asked` look answered → silent under-block. Fix direction: match pending on
`(turnId, questionId)`, not `questionId` alone.

B2 (hard blocker) — removing the active-goal gate for `phase==="I"` breaks the L6 invariant
"a plain interactive session pauses for the human" (guard 2b, hook.ts:400; loop/SKILL.md:33).
In the canonical interactive interview (no goal), the model asks via `request_user_input` and
ends its turn; the I-branch would return `decision:"block"` and re-prompt the model instead
of yielding to the human. The branch arms continuation in exactly the no-goal case that must
NOT continue.

Deeper contradiction (the reason to stop and ask): the design's motivation — "drive the
interview to completion autonomously" — only applies under an ACTIVE goal. But when a goal is
active, `request_user_input` is hard-denied upstream (suppressesInterview = active|unreadable,
goal-active.ts:88), so no `question_asked` is ever recorded and the pending-question trigger
can never fire on the goal path. So:
- goal active  → interview suppressed → no pending question → guard has nothing to act on;
- goal inactive → interactive → guard MUST pause for the human (L6), not block.
Both domains reject the pending-question guard. The I-phase Stop guard as specified has no
valid domain. This is the contradiction surfaced back to the user.

Secondary: unknown contradiction severity reconstructs to `high` (interview.ts:109), so a
malformed tracker array would also trip the high-contradiction trigger; if the guard survives
redesign it must route through the single MAX_STOP_BLOCKS cap (no parallel return) and treat
`unreadable` as release (fail-safe).

## Decision needed (sent to user)

The rescan-coordinator helper (signals only) is safe and still planned. The I-phase Stop
guard needs a direction from the user before implementation — see the turn message. Options
considered: (a) drop the Stop-guard half and keep only the rescan-coordinator + a
UserPromptSubmit interview-resume directive; (b) gate the I-branch on active goal AND a
future "goal-mode interview" that lifts the request_user_input deny; (c) redefine where the
autonomous interview lives. No code written until resolved.
