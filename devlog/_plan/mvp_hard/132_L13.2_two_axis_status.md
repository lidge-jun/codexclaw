# L13.2 / 132 — WP3 Design: 2-axis status migration (D3')

Status: DONE (shipped + tested) · 2026-06-30 · parent [130](130_L13_truthfulness_interview_impl.md)

Interview-confirmed: `DONE` must mean shipped+tested. mvp_hard collapsed plan-completion and
runtime-completion into one `DONE`, so L9/L11/L12 read as shipped when only the plan/decision
shipped. Fix = express the two axes the LOCKED mvp_res legend already separates.

## Migration

### 1. Ledger table → two status columns
`devlog/_plan/mvp_hard/000_INDEX.md` ledger header becomes:
`| Ln | decade | scope | decision-state | impl-state |`
- `decision-state`: from the LOCKED legend — DONE/FROZEN/PLANNED/ANALYZED/DEFERRED/BLOCKED.
  For these loops it is DONE (the decision/plan IS closed).
- `impl-state`: DONE only when code shipped+tested; otherwise PLANNED (decided, impl pending)
  or DEFERRED (intentionally postponed).

### 2. Per-row truth (decision-state | impl-state)
- L1 audit: DONE | DONE (the audit artifact IS the deliverable).
- L2-L8: DONE | DONE (code shipped + tested; L2-L7 FSM/CLI/Stop, L8 truth sweep).
- L9 subagent/model hardening: DONE | PLANNED (plan closed, spawn-wrapper/​catalog runtime deferred).
- L10 memory/chat parity decision: DONE | DONE (it was a *decision* loop; the decision shipped,
  and chat-search was retired in WP1 — no deferred runtime remains for L10's scope).
- L11 developer docs website: DONE | PLANNED (SoT reconciled, but the docs *website* is not built).
- L12 skill-internal hardening: DONE | PLANNED — NOTE WP2 just shipped the interviews JSONL +
  scan-evidence + soft-gate that L12 had listed as deferred; record that the L13 loop delivered
  that slice, with remaining PostToolUse answer-capture still PLANNED.
- L20: PROPOSED | PROPOSED (backlog).

### 3. COMPLETE prose
The "L2-L11 COMPLETE" aggregate and per-line "L9/L11/L12 COMPLETE" claims are rewritten so they
never assert impl-completion for plan-only loops. Aggregate becomes "L2-L8 shipped+tested; L9/L11/L12
decision-closed with runtime PLANNED; L10 decision-closed".

### 4. README + roadmap.html
- `README.md:44` "L2-L11 ... active" → reflect shipped vs plan-only split.
- `roadmap.html`: it self-declares "generated from the canonical ledgers", so it is NOT excluded.
  Either (a) correct its L9/L11/L12 badges to a two-tier (decision DONE / impl PLANNED) presentation,
  or (b) regenerate. Choose (a) minimal: add an `impl` badge alongside `done` for those rows, and
  fix the footer legend. Keep mvp_res-track rows untouched (different namespace, correctly DONE).

### 5. Existing drift (separately real)
L2-L4 INDEX says DONE but loop docs `020/030/031/040` say `Status: P`. Per the legend, `P` is an
active-pass label, not a loop status — but these loops shipped, so their Status lines should read
`DONE` (impl) to stop contradicting the ledger. Fix the four headers.

## Acceptance (C)
1. mvp_hard ledger has two status columns; every row carries both axes.
2. No surface claims impl-DONE for L9/L11/L12 (they read decision DONE | impl PLANNED).
3. L12 row credits the WP2-shipped slice; remaining capture still PLANNED.
4. COMPLETE prose + README + roadmap.html consistent with the ledger (no aggregate impl-DONE lie).
5. roadmap.html footer legend matches the two-axis model; mvp_res rows untouched.
6. `git diff --check` clean; full suite still green (docs-only, no code regression).
