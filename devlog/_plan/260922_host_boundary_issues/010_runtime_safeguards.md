# wp1 — invocation evidence and automation ownership

Depends on wp0. Before: doctor hashes trust declarations but cannot show invocation; automation_update has no plugin ownership guard. After: separate per-session invocation evidence and narrow fail-closed heartbeat mutation checks. No universal host authorization claim.

## Observation worker write map

NEW scripts/hook-observation.mjs and components/cxc-ops/test/hook-observation.test.ts under plugins/codexclaw. This shared leaf uses the stable scripts path, importable at the same relative path from src and dist; this is a new observation owner, not an existing manifest-validator location. Pure fs/path/crypto owner exports recordHookInvocation(raw, component, event, entrypoint) and readHookObservations with explicit filtering arguments. Record schemaVersion=1, sessionId, nullable agentId, component, event, observedAt, pluginRoot/version, entrypoint digest, manifest digest and outcome='invoked'. No prompt, tool input, token or full payload is stored. Sanitize IDs by hashing for filesystem paths; bound parsed raw size and metadata. Atomic unique temporary file -> per-session/actor/component-event latest record (root and child never overwrite each other); no shared append race. Storage failure must not alter handler stdout/exit. Reader validates schema, session, agent, root/version/manifestDigest/entrypointDigest, time freshness and future timestamps. Missing/corrupt/mismatched/old evidence is unverified, not successful.

MODIFY cxc-ops/src/doctor.ts: add hook-execution separate from hook-trust. Report observed identities and unknown/unverified coverage; one invocation does not prove all hooks fired. Current native session is default filter; absent session produces WARN. Tests use isolated homes and explicit session dependencies.

MODIFY existing hook ingress in pabcd-state/src/cli.ts, cxc-ops/src/cli.ts, recall/src/cli.ts, bg-wake/src/cli.ts, config-guard/src/cli.ts, provider-bridge/src/cli.ts and subagent-config/src/spawn-attach-hook.ts direct CLI main plus subagent-config/src/fallback-dispatch-cli.ts SessionStart path (eight entrypoints across seven components). Read stdin once, call leaf helper, continue unchanged. Existing registrations remain unchanged. Import ../../../scripts/hook-observation.mjs from component src/dist; build rewrites only .ts extensions, so cross-component /src/ imports are forbidden here. No wrapper process. Build-derived dist files regenerate. Update compiled test fixtures that copy selected payload files so the shared .mjs helper is present; packaged-install smoke must resolve it.

MODIFY cxc-ops doctor tests and compiled hook tests to assert invocation without claiming handler success. Reachable cases: real child process running registered CLI command with isolated payload records invocation; direct function import does not; malformed/oversized payload and unwritable store preserve behavior; concurrent different invocations do not lose other records; stale/root/digest/session/agent mismatches remain unverified.

## Automation worker write map

NEW pabcd-state/src/automation-ownership-gate.ts and test/automation-ownership-gate.test.ts. Parse exact automation_update tool aliases at PreToolUse. Unrelated tools and mode=view pass without store access. For mutations, validate object/mode/id and native caller session; child agent identity must not gain parent ownership. Read bounded regular non-symlink automation TOML at CODEX_HOME/automations/<safe id>/automation.toml. Reject malformed/duplicate/conflicting ownership, path traversal, missing file, unknown kind and cron lacking ownership. A strict supported flat-TOML reader must ignore multiline prompt content and reject ambiguity rather than regex-match fake owner text inside a prompt. Reuse existing parser only if it preserves this contract; no new dependency solely for parsing.

Existing heartbeat update/delete requires matching target_thread_id; create/suggested_create heartbeat may omit target to use caller, or explicitly name caller only. Reject retargeting to another task and invalid create-with-id. Unknown mutating modes fail closed. Denials return normal PreToolUse deny envelope. No direct automation mutation or prompt backup is introduced.

MODIFY pabcd-state/src/cli.ts immediately before the subagent early exit: dedicated automation gate branch, errors on matched mutation deny. Observation worker and automation worker must not edit cli.ts concurrently: main integrates the automation branch after observation worker returns. NEW hooks/pre-tool-use-guarding-automation-ownership.json; MODIFY plugin manifest registration and hook count fixtures by main after workers. Match known native alias forms only, not arbitrary source-code text.

Tests: own delete/update allowed; foreign/missing/malformed owner denied; child denied; views unaffected; explicit foreign create/retarget denied; unrelated tools pass; multiline fake owner, duplicate key, traversal, symlink, unknown modes rejected. Compiled registered-hook test must exercise denied foreign ownership using temporary store.

## Boundaries and verification

E1-style executing PreToolUse command can deny only matching calls when loaded. Known bypass: nested Code Mode calls without inner hooks, disabled/untrusted hooks, UI, shell/file writes, ownership TOCTOU. Final host enforcement layer: none. Wording is scoped safeguard, not host-wide enforcement. Invocation records are same-user writable diagnostics and replayable, not host attestations.

Focused commands: node plugins/codexclaw/scripts/test.mjs with the two new test paths and existing cxc-ops/doctor and hook-e2e tests. Existing runner target arguments directly name changed tests. npm run build observes component src; node plugins/codexclaw/scripts/gate.mjs observes manifest/inventory/docs. New tests first run after creation. Main updates structure/INDEX.md and docs-site reference/hooks.md, then inventory count after a measured full suite. Immutable historical audit records are preserved.

Implementation clarification: update skills/loop/references/waiting.md with the operational ownership preflight (read exact target association, never infer duplicates from suffixes, prefer supported in-place update). This is the same D5 scoped guidance, not a new enforcement surface. The compiled automation test was run against the unchanged pre-fix dist: it failed because stdout had no deny envelope (Unexpected end of JSON input), proving the regression test observes the missing gate. Temporary stores only.

Test-isolation clarification: scripts/test.mjs also sets scratch CODEX_HOME and CODEX_SQLITE_HOME alongside existing CODEXCLAW_HOME. New observation side effects must not let existing integration fixtures reach the operator home. This is a required test harness isolation change; production behavior is unchanged.
