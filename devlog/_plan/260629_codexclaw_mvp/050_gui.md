# 050 — GUI

Status: TODO

## Goal
Local web dashboard (Vite + React) for subagent config + prompt tuning + ocx link bar.

## Pages
- Subagents: per-role default/multi-model selection + prompt override editor.
- Prompts: system/role prompt tuning.
- Provider link bar: when ocx detected, link to `http://localhost:10100`; hidden otherwise.

## Notes
- Mirror the opencodex GUI stack (Vite+React) for familiarity.
- Launched via `codexclaw gui`.
- Reads/writes through the `subagent-config` component.

## Verify
- GUI loads, edits persist to `.codexclaw/subagents.json`.
- Link bar appears only when ocx is up.
