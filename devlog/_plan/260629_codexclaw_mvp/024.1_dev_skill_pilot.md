# 024.1 — dev-* Conversion Pilot (dev-debugging)

Status: TODO  ·  Phase 1

## Goal
Convert ONE skill end-to-end first to lock the conversion recipe before bulk porting.

## Why dev-debugging
Self-contained, high-value, low cross-dependency on cli-jaw runtime.

## Steps
1. Copy cli-jaw `skills_ref/dev-debugging/SKILL.md`.
2. Apply 024 conversion rules.
3. Add `agents/openai.yaml` if routing benefits.
4. Install locally; verify it routes on a debugging prompt.
5. Capture the diff as the canonical "conversion recipe" for 060 bulk migration.

## Verify
- Routes on "this is failing, find the root cause"-style prompts.
- Contains zero cli-jaw-specific commands/paths.
