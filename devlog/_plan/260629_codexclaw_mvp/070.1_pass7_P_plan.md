# 070.1 — Pass 7 (P): Build Aggregation (T-070) + Phase-1 Verification Gate (029) — execution plan

Status: P (PLANNING) · Phase 1 · Loop Pass 7/7 (final, see 017) · executes T-070 + 029 (S1–S5).
Unit: turn the build STUB into a real, reproducible `scripts/build.mjs` that compiles each
component `src/*.ts` → `dist/*.js` and validates the aggregated single-plugin layout, then run the
029 verification gate and close Phase 1.

## Ground truth (source-verified this phase)
- Node runtime is **v24.14.1**; `node <file>.ts` runs natively via type-stripping (verified: a `.ts`
  with `const x: number` ran and printed). `node:module` exposes **`stripTypeScriptTypes`** (verified
  present) — a zero-dependency compile path. **No `tsc` and no `esbuild` are installed** (verified
  `npx --no-install` both fail) → the build must NOT depend on an external TS toolchain.
- The plugin manifest already references compiled outputs:
  - `plugin.json` hooks → `.../components/pabcd-state/dist/cli.js` (3 hooks; verified file content).
  - `.mcp.json` → `./components/subagent-config/dist/mcp.js` (verified).
  - omo uses the identical pattern: `node "${PLUGIN_ROOT}/components/<c>/dist/cli.js" hook <event>`
    (verified in `devlog/.lazycodex/plugins/omo/hooks/*.json`).
  - ⇒ each component needs a real `dist/` whose entry filenames match those references.
- Component source uses **`./x.ts`** relative import specifiers (verified via rg across
  pabcd-state + config-guard). Node's type-stripping runs `.ts` fine in-place, but emitted `.js`
  files importing `"./state.ts"` would be wrong → the build must rewrite `.ts` specifiers → `.js`.
- Components present: `pabcd-state` (cli.ts/fsm.ts/goal-gate.ts/hook.ts/parse.ts/state.ts),
  `config-guard` (activate/cli/deactivate/features), `provider-bridge` (cli.ts),
  `subagent-config` (mcp.ts). All ESM, `"type":"module"`, `node:*` only.
- `gui/` is a separate workspace (README + src) — NOT part of the hook/MCP runtime path; OUT of the
  build-aggregation scope for Pass 7 (Phase 2 concern).

## Design decision: compile with node:module.stripTypeScriptTypes, no external toolchain
`scripts/build.mjs` (real) will, for each component under `components/*` that has `src/`:
1. Read every `src/**/*.ts`.
2. `stripTypeScriptTypes(source, { mode: "strip-only" })` to get JS (keeps line layout; no downleveling
   needed on Node 24).
3. Rewrite relative import/export specifiers ending in `.ts` → `.js` (regex on
   `from "..."` / `import("...")` with a leading `./` or `../`).
4. Write to `components/<c>/dist/<same-relative-path>.js`.
5. Skip `test/` (tests run on `.ts` directly via `node --test`).
Rationale: matches the existing manifest references (dist/*.js), needs zero new dependencies, and is
fully reproducible (same input → same output). A `tsc`/`esbuild` path would add a toolchain the repo
deliberately avoids (components have no build step today and run `.ts` natively).

### Validation in the same script (or a sibling)
After emitting dist, the build verifies the aggregated layout:
- `plugin.json` parses, has NO `[TODO]`/placeholder strings, and every `hooks[*]` path + the
  `.mcp.json` mcp args path exists on disk after build.
- `skills` dir exists and every skill dir has a `SKILL.md`.
- Each component dist entry referenced by a manifest exists.
Emit a clear PASS/FAIL summary + nonzero exit on failure (so it can gate CI later).

## Build aggregation note (skills/hooks/agents already at plugin root)
codexclaw already keeps `skills/`, `hooks/`, `agents/` directly under the plugin root (unlike omo's
monorepo that copies component dist→root). So "aggregation" here = (a) compile components→dist in
place, (b) validate the single-plugin layout. No copy step is required; document this divergence from
omo explicitly so it isn't mistaken for a missing step.

## Scope boundary
- IN:
  - Replace `plugins/codexclaw/scripts/build.mjs` stub with the real compile+validate implementation.
  - Generate `dist/` for the 4 components (pabcd-state, config-guard, provider-bridge, subagent-config).
  - A `test/build.test.mjs` (node:test, `.mjs`) asserting: build is idempotent (run twice → same bytes),
    every manifest-referenced dist file exists post-build, `.ts` specifiers were rewritten to `.js`,
    compiled `pabcd-state/dist/cli.js` actually runs a hook end-to-end (spawn `node dist/cli.js hook
    user-prompt-submit` with a sample payload and assert exit 0 + expected stdout shape).
  - Run 029 gate S1–S5 + write per-step `*_done.md` notes; flip STATUS Phase-1 rows + 029/070 → DONE.
- OUT: GUI build (Phase 2); marketplace publish/`codex plugin add` against the network (document the
  commands, do not execute a real remote install); provider/ocx runtime (Phase 2); downleveling for
  older Node (target is pinned Node 24).

## 029 gate mapping (how each S-item is proven this phase)
- **S1 install + skill discovery**: validator confirms `skills/` + every `SKILL.md`; frontmatter already
  C-gate-verified in Pass 4. Evidence = validator output + skill count.
- **S2 IPABCD state transition P→A**: run compiled `pabcd-state/dist/cli.js hook user-prompt-submit`
  with an explicit `orchestrate A`-style trigger payload in a temp CODEX/transcript dir; assert the FSM
  advances + a ledger entry is written. (pabcd-state node:test 52/52 already covers the logic; this
  proves the COMPILED artifact works.)
- **S3 pilot dev skill routes**: assert dev hub skill `allow_implicit_invocation:true` +
  on-demand routers false (already Pass 4); spot-check 1–2 SKILL.md frontmatter via the validator.
- **S4 config.toml byte-identical (027 guard)**: config-guard test already proves offline backup/revert +
  drift guard with real `~/.codex` untouched; re-run config-guard node:test as the S4 evidence.
- **S5 subagent spawns on default model**: assert `agents/*.toml` parse with `model` unset/default +
  README inline-injection contract (Pass 5); no live spawn needed (documented as runtime behavior).
- **build aggregation reproducible**: `build.test.mjs` idempotency test.
- **no `[TODO]` placeholders in plugin.json**: validator greps the manifest.
- **devlog updated**: STATUS + per-step done notes.

## File change map
- M `plugins/codexclaw/scripts/build.mjs` (stub → real compile+validate)
- A `plugins/codexclaw/components/{pabcd-state,config-guard,provider-bridge,subagent-config}/dist/**` (generated)
- A `plugins/codexclaw/test/build.test.mjs` (node:test .mjs)
- A `devlog/_plan/260629_codexclaw_mvp/029.1_phase1_verification_done.md` (S1–S5 evidence notes)
- M `devlog/_plan/260629_codexclaw_mvp/029_phase1_verification.md` (checklist → done)
- M `devlog/_plan/260629_codexclaw_mvp/070_packaging.md` (status → done for build aggregation)
- M `devlog/_plan/260629_codexclaw_mvp/STATUS.md` (029/070 + Phase-1 row → DONE; Pass-7 progress line)

## dist policy (commit or ignore?)
Decision: **commit `dist/`** for Pass 7 so the marketplace install path works without a post-clone build
(omo ships built artifacts the same way). Build is reproducible + idempotency-tested, so committed dist
can't silently drift from src (the test catches it). Revisit (gitignore + prepack build) only if it
becomes noisy — noted as a Phase-2 follow-up.

## Key invariants
- Build needs ONLY Node 24 (no tsc/esbuild/network). Reproducible + idempotent.
- Every manifest-referenced path (hooks, mcp, skills) resolves on disk after build.
- Compiled artifacts behave identically to the `.ts` source (proven by running a compiled hook).
- pabcd-state node:test stays **52/52**, config-guard **15/15** (regression guard; no src logic change).
- Precise skills gate stays empty.

## Accept criteria (Pass 7 done = C/D, and Phase 1 closed)
- `node plugins/codexclaw/scripts/build.mjs` exits 0, prints a PASS summary, emits dist for all 4 components.
- `build.test.mjs` green: idempotent build, manifest paths exist, `.ts`→`.js` rewrite, compiled hook runs.
- config-guard 15/15 + pabcd-state 52/52 still green.
- 029 S1–S5 all checked with evidence notes; no `[TODO]` in plugin.json; precise gate empty.
- STATUS Phase-1 row + 029 + 070 → DONE; orchestrator IDLE after D; goal has no remaining work-phases.

## A-phase audit ask (next: A gate, parallel subagent)
Audit this plan against codex-rs plugin loading + omo build pattern: (1) is `node:module`
stripTypeScriptTypes a sound, supported build path for shipped plugin dist (vs needing tsc)? (2) does
codex's plugin loader actually execute the `dist/*.js` hook/mcp paths as referenced, or does it support
`.ts` directly (which would let us drop the build)? (3) is committing dist the right call for the
marketplace path? (4) any S1–S5 item that needs a stronger/live proof than planned?
