# 025.1 — Pass 5 (P): Subagent Roles (B-opt2 inline) — execution plan

Status: P (PLANNING) · Phase 1 · Loop Pass 5/7 (see 017) · executes step 025.
Unit: T-025 — make codex subagents behave like cli-jaw employees via named roles whose
developer_instructions reference the converted dev-* router skills.

## Ground truth (source-verified this phase)
- codex-rs `core-plugins/src/loader.rs`: plugin manifest exposes ONLY `skills`/`hooks`/`mcpServers`
  (loader.rs:629, manager.rs:232-236) — there is NO `agents` field. Plugins do not auto-register agent roles.
- Agent roles are discovered per config LAYER at `<config_folder>/agents/*.toml`
  (`core/src/config/agent_roles.rs:71-98` discover_agent_roles_in_dir + declared roles in `agents.toml`),
  parsed by `parse_agent_role_file_contents` (role.rs). Built-ins: explorer, awaiter/worker.
- Therefore plugin-shipped `plugins/codexclaw/agents/*.toml` are NOT picked up by codex as live roles.
  → Phase 1 = **B-opt2** (019.2): the `.toml` files are the canonical SOURCE; the main agent injects
  their `developer_instructions` INLINE in `spawn_agent({agent_type, message:"TASK: ..."})`, omo's proven pattern.
- `spawn_agent` supports built-in `agent_type` (explorer for read-only, worker/default for writes) + an
  optional `model` override (Phase 2 multi-model basis). Roles map onto these built-ins.

## Scope boundary
- IN: enrich the 3 stub role files `plugins/codexclaw/agents/{explorer,reviewer,executor}.toml` into the
  omo-shape (name, description, nickname_candidates, model="default", optional model_reasoning_effort,
  developer_instructions triple-quoted) whose instructions reference the dev-* routers per surface.
  Add a short README at `plugins/codexclaw/agents/README.md` documenting B-opt2 (inline injection, not
  auto-registered) + the explorer→read-only / executor→worker mapping + Phase-2 model-override hook.
- OUT: live role auto-registration (blocked by codex manifest — would need config-layer install, deferred),
  multi-model per-role selection (Phase 2, GUI/ocx), any .ts/source changes, hook changes.

## File change map
- M `plugins/codexclaw/agents/explorer.toml` — add developer_instructions (read-only investigation;
  reference dev-architecture + dev-debugging routers; output = specific findings w/ file:line).
- M `plugins/codexclaw/agents/reviewer.toml` — add developer_instructions (adversarial diff/plan review;
  reference dev-code-reviewer + dev-security; output = PASS/FAIL + blockers w/ file:line). nickname_candidates.
- M `plugins/codexclaw/agents/executor.toml` — add developer_instructions (bounded code change in a disjoint
  write scope; reference dev-frontend/dev-backend/dev-testing per surface + dev §0 classifier; commit nothing,
  report changed files). nickname_candidates.
- A `plugins/codexclaw/agents/README.md` — B-opt2 explanation, role→built-in map, inline-injection contract,
  Phase-2 model override note, and the codex-rs ground-truth citation.

## Accept criteria (Pass 5 done = C/D)
- 3 role .toml files valid TOML, each with name/description/developer_instructions; each names the dev-* router(s)
  for its surface; model="default" (Phase 1).
- README documents the B-opt2 inline contract + role→agent_type mapping + that roles are NOT auto-registered.
- Precise gate empty on agents dir:
  `rg -n "cli-jaw|orchestrate [IPABCD]\b|dispatch --agent|bgtask|verify-counts|/Users/jun/\.cli-jaw|jaw employee" plugins/codexclaw/agents/` = EMPTY.
- node:test still 52/52 (no .ts touched — regression guard).
- STATUS.md updated (025 DONE).

## A-phase audit targets (parallel subagent)
- Confirm the codex-rs claim that plugin manifests cannot register agent roles (no `agents` field) — so B-opt2
  is the correct Phase-1 path, not an avoidable shortcut.
- Confirm each role's developer_instructions reference the RIGHT dev-* routers for its surface and contain no
  cli-jaw runtime leakage.
- Confirm the explorer→read-only / executor→worker mapping matches spawn_agent's actual agent_type options.
