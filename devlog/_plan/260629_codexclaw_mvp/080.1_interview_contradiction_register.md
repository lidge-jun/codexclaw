# 080.1 — Pass 8 인터뷰 모순 레지스터 (dry-run #1)

Status: AUDIT (dry-run) · gpt-5.5 서브에이전트(Contrarian/Socratic/Ontologist 역할)로 080 플랜을 검증

> ooo 구현이 없어서 Pass 8 인터뷰 루프를 **수동 dry-run**: gpt-5.5 Mind가 모순만 도출(질문·수정 X),
> 메인이 이를 질문으로 변환(D2 그대로). 상위: [080_pass8_interview_hardening_plan.md](080_pass8_interview_hardening_plan.md) ·
> 모델주입: [034.5_mind_model_injection.md](034.5_mind_model_injection.md)

## 모순 15건 (consequence 순, gpt-5.5 dry-run 2026-06-30)
| # | axis | 모순 요약 | 강제 결정 | 상태 |
|---|------|-----------|-----------|------|
| 1 | D5 frozen spec | D3(메인이 devlog 직접수정) vs D5(종료 시 불변 freeze) — 무엇이 얼고 devlog가 정본인지 불명 | 무엇을 freeze하나, devlog가 정본인가 | ✅R-A: 플래닝 파일=정본 |
| 2 | D5 path | frozen spec 포맷·경로·load 미정 → goal 게이트가 못 찾음 | artifact 경로·스키마·goal-load 계약 | ✅R-A: `.codexclaw/plan/` |
| 3 | spawn 계약 | "spawn_agent 우선 + NL fallback" — ooo는 codex에서 spawn 금지인데 우선이라 모순 | codex 디스패치 단일 계약 선택 | ❓JUN |
| 4 | correlation | MCP 없는데 Mind별 결과 귀속 보장 수단 없음 | MCP 독립 correlation envelope 정의 | 기계적 |
| 5 | 5 vs 라우팅 | 5-Mind 전체 커버 vs 1~2개만 호출 — ready를 일부만 보고 선언 가능 | ready에 5 전부 필요한가 라우팅만인가 | ✅R-B: 규모적응 라우팅 |
| 6 | per-Mind role | 032 role은 explorer/reviewer/executor만 — Mind 5종이 first-class role인가 | Mind를 설정가능 role로 승격? | ❓JUN |
| 7 | model 부재 | ocx 없으면 default-only인데 per-Mind 모델 지정과 충돌 | 모델 부재 시 fail/strong-default/Mind disable | ❓JUN |
| 8 | multi-model 결정성 | Mind별 다른 모델 → severity 캘리브레이션 불일치, ready가 모델 편차에 흔들림 | cross-model 정규화 규칙 | 기계적 |
| 9 | assumption vs ready | auto가 assumption 기록하는데 ready는 assumptions==0 요구 → 교착 | assumption 생애주기(pending/accepted/frozen) | 기계적+JUN |
| 10 | silent auto vs review | medium 자동해결+직접수정 → 유저가 freeze 전 검토·번복 기회 없음 | freeze 전 assumption ledger 리뷰 단계? | ❓JUN |
| 11 | /goal hint 우회 | 인터뷰 후 goal 원칙 vs /goal은 hint만으로 시작 가능 → frozen spec 우회 | spec 없는 /goal: 거부/degrade/암묵spec | ✅R-C: /goal 직접 허용 |
| 12 | hard ban vs advisory | 022.3는 goal 내 인터뷰 "절대금지"인데 hook 페이로드에 goal-active 없어 advisory만 | Phase1 advisory 수용 vs hard 주장 보류 | 기계적 |
| 13 | fail-fast vs fail-safe | 인터뷰는 request_user_input hard 의존인데 cli.ts는 모든 에러 삼키고 exit0 | hook 생존(fail-safe) vs 인터뷰 불가 보고(fail-fast) 분리 | 기계적 |
| 14 | session vs artifact | 상태는 per-session인데 frozen spec은 후속 goal/세션이 소비 → 세션 경계서 핸드오프 유실 | session-scoped tracker vs project-scoped spec 소유 | ❓JUN |
| 15 | idempotency | 현 idempotency는 주입 1회만 — subagent 디스패치/assumption/plan edit/freeze 미보호 | round/contradiction/edit/freeze별 idempotency 키 | 기계적 |

## 처리 방침
- "기계적" = Pass 8 구현 시 코드 결정(별도 jun 승인 불요). 위 레지스터를 구현 체크리스트로 사용.
- "❓JUN" = product/구조 결정 → 080의 OPEN 섹션 + 별도 질문으로 승격(이 dry-run 직후 질문 던짐).
- 다음 dry-run 라운드(Evaluator/Simplifier Mind)는 이 결정들이 반영된 뒤 재실행.

## 상태
- 2026-06-30: dry-run #1 완료(gpt-5.5). 15건 기록. JUN 결정 대기 → 반영 후 080 갱신.
- 2026-06-30: JUN 결정 R-A/R-B/R-C 반영(모순 #1/#2/#5/#11 해소). 잔존 ❓JUN = #6,#7,#10,#14 +
  기존 080 OPEN #3/#5/#6. 기계적 = #4,#8,#9,#12,#13,#15(구현 체크리스트).

## dry-run #2 (gpt-5.5 Carver, 재활용 agent, fresh-eyes 강제 — 2026-06-30)
R-A/R-B/R-C 자체를 원점 재심사 + 잔존 항목 재도출. 10건:
| # | axis | 모순 요약 | 강제 결정 | 상태 |
|---|------|-----------|-----------|------|
| R2-1 | freeze vs /goal anytime | 플랜파일이 live-edit 정본인데 /goal은 언제나 진입 가능 → 미완성 인터뷰 플랜이 정본으로 소비 | draft/review/frozen 상태 구분 or live 실행 위험 수용 | ❓JUN |
| R2-2 | R-A 불변성 | "freeze=플랜파일"은 스냅샷/해시/버전경계 없음 — 가변파일은 read-only frozen 아님 | freeze가 의미적 관례인가 강제 artifact인가 | ❓JUN |
| R2-3 | session vs project | tracker는 per-session, 플랜은 project-wide — readiness/assumption을 특정 플랜 revision에 묶는 binding 없음 | session tracker ↔ project 플랜 소유/링크 정의 | ❓JUN |
| R2-4 | assumption 생애주기 | auto가 assumption 기록하나 ready는 assumptions==0 — 어떻게 accepted/cleared되는지 미정 | canon화 전 assumption 생애주기 정의 | 기계+JUN |
| R2-5 | Mind role 분류 | 032=explorer/reviewer/executor vs 034.5=mind-* — first-class면 taxonomy/GUI 확장, overload면 per-Mind 설정 상실 | first-class role vs pseudo-role | ❓JUN |
| R2-6 | 모델 부재 상태 | 033 ocx부재=[main]인데 034.5는 per-Mind 명시모델 — invalid/unavailable/disabled/downgraded 구분 없음 | fail-fast/strong-default/disable 택1 | ❓JUN |
| R2-7 | 라우팅 vs self-cert | ready는 차원기반인데 Mind 호출은 재량 → 1개만 불러 점수 올리고 ready 선언 가능(자기인증 붕괴) | readiness level별 최소 coverage/근거 | ❓JUN |
| R2-8 | fresh-eyes 재활용 | 같은 agent 재활용+원점요구 — 프롬프트 리셋이 anchoring 못 지움(특히 resolved 재질문 시) | stateless agent vs 명시적 anti-anchoring 근거 | 기계+JUN |
| R2-9 | goal-mode hard ban | 022.3는 hook이 goal 내 인터뷰 suppress하라는데 080은 payload에 goal-active 없어 advisory만 → "hard ban"이 프롬프트 안내일 뿐 | 강제 가드 구현 vs 주장 downgrade | 기계 |
| R2-10 | prompt-only 엔진 | 핵심 루프(spawn/triage/plan edit/tracker/flags)가 코드 아닌 메인 행동 규약 — hook.ts는 텍스트 주입만 → 상태전이가 agent 준수 의존, idempotent/audit 불가 | 어느 루프 단계를 실행 가능 기계로 만들지 | ❓JUN |

- 핵심 통찰: R2-1/R2-2/R2-3는 한 뿌리 — "플랜파일=정본"이라면 draft↔frozen 경계와 revision 식별이 필요.
- R2-10은 메타 결정: 인터뷰 엔진을 "행동 규약"으로 둘지 "코드 기계"로 둘지 (Pass 8 구현 강도).

### dry-run #2 JUN 결정 (2026-06-30) → 080 RESOLVED #3
- R-D [R2-1/2/3 해소]: codex update_plan은 파일 영속화 X(실측) → codexclaw가 `.codexclaw/plan/` 정본 루트
  직접 운영. 플랜=정본 + manifest 해시로 freeze 시점 식별. 가벼운 goal은 시작 시 서브에이전트 backfill로 보강.
- R-E [R2-10 해소]: 핵심 상태전이(tracker/flags/해시/freeze)만 코드, 인지 루프(모순도출·질문·편집)는 행동 규약.
- R-F [R2-5 해소]: MVP=인라인 고정 프롬프트 Mind(모델 지정 X), Phase 2 GUI 때 first-class role 승격(034.5).
- 잔존 ❓JUN: R2-4(assumption 생애주기), R2-7(라우팅 vs 최소 coverage) + 080 OPEN #3/#5/#6.
- 기계(구현 체크리스트): R2-8(anti-anchoring 근거 남기기), R2-9(goal-mode advisory→가능시 hard).

## dry-run #3 (gpt-5.5 Carver 재활용, fresh-eyes — 2026-06-30)
R-D/R-E/R-F 자체를 원점 재심사. 9건:
| # | axis | 모순 요약 | 강제 결정 | 상태 |
|---|------|-----------|-----------|------|
| R3-1 | ⭐backfill paradox | R-D "가벼운 goal은 시작 시 서브에이전트 backfill" vs D4/022.3 "goal 모드 인터뷰 금지" — backfill이 곧 goal 안 인터뷰 | backfill=자율검증만인가 인터뷰/spec강화 단계인가 | ❓JUN |
| R3-2 | assumption 생애주기 | assumption 기록되나 ready는 0 요구 — 누가 무슨 근거로 clear? R-E라 self-attest 위험 | assumption 상태+frozen 전환 근거 정의 | ❓JUN |
| R3-3 | 해시 무결성 | freeze=manifest 해시인데 R-E라 편집은 행동기반 — freeze 후 편집 차단/감지/재계산 규칙 없음 | frozenAt 후 mutation 정책·검증 시점 | 기계+JUN |
| R3-4 | 최소 coverage | R-B+R-E면 메인이 Mind 1개 불러 점수 올리고 freeze 가능 — 독립검증 없음 | readiness level별 최소 coverage/근거 | ❓JUN |
| R3-5 | freeze 전 리뷰 | manifest 경계는 있으나 assumption 리뷰 게이트 없음 — 미검토 추정이 정본으로 해시됨 | freeze에 assumption 리뷰 강제? | ❓JUN(=R2-4계열) |
| R3-6 | flags.interview 주체 | OPEN#6 미정인데 canEnter("P")가 이 flag 의존 — 자동이면 점수인플레로 진행, 명시면 정체 | flag 설정 actor+근거 | ❓JUN |
| R3-7 | elicitation fallback | OPEN#5 미정 — flag off 시 enable 권유 vs MCP elicitation, fail-fast와 겹쳐 비결정적 | 단일 순서 정책 | ❓JUN |
| R3-8 | Contrarian 톤 | OPEN#3 — 톤이 severity 좌우 → auto-assume vs escalate 분기 바꿈(UX 아닌 readiness 영향) | Contrarian severity 캘리브레이션 | ❓JUN |
| R3-9 | R-F phase 이행 | MVP 인라인 Mind vs Phase2 first-class role — 이름/결과 매핑 계약 없어 frozen manifest 재현 불가 | 안정적 Mind 식별자+provenance 사전 정의 | 기계 |

- ⭐ R3-1이 최우선: "가벼운 goal backfill"과 "goal 내 인터뷰 금지"는 직접 충돌. 정의 안 하면 hard ban 우회.
- R3-2/R3-5는 한 뿌리(assumption이 검토 없이 정본화). R3-3/R3-9는 freeze 재현성/무결성.

### dry-run #3 JUN 결정 (2026-06-30) → 080 RESOLVED #4 (테마: 가볍고 신뢰 기반)
- R-G [R3-1 해소]: backfill = 자율 검증만(유저 질문 X) → 인터뷰 아님, goal 내 인터뷰 금지와 무충돌.
  구분 기준 = "유저 질문 여부".
- R-H [R3-2/R3-5/R2-4/#9 해소]: assumption 있어도 ready 허용 — frozen spec의 `## OPEN ASSUMPTIONS`로
  명시. readiness = 4차원 충족 + 모든 assumption 명시기록(assumptions==0 엄격 폐기). 교착 제거.
- R-I [R3-4/R2-7 해소]: 최소 coverage 강제 없음 — 메인 재량 신뢰(R-E 일관). self-cert 수용,
  OPEN ASSUMPTIONS가 빈약 spec 위험의 유일한 투명성 안전장치.
- 잔존 ❓JUN: R3-3(해시 mutation 정책 — 기계 가까움), 080 OPEN #3(Contrarian 톤)/#5(elicitation)/#6(flags 주체).
- 기계: R3-9(Mind 안정 식별자), R3-6(flags.interview는 #6 결정 후 구현).

## dry-run #4 (gpt-5.5 Carver 재활용, fresh-eyes — 2026-06-30)
R-G/R-H/R-I 원점 재심사. 9건:
| # | axis | 모순 요약 | 처리 |
|---|------|-----------|------|
| R4-1 | 해시 mutation 정책 | freeze 후 플랜 편집 시 stale manifest vs 현재파일 — 검증 시점/동작 미정 | ✅기계 R-J |
| R4-2 | R-H 구현 드리프트 | 080 본문에 옛 assumptions==0 계약이 5곳 잔존 | ✅FIXED(문서 정규화) |
| R4-3 | OPEN ASSUMPTIONS 소비 | R-H가 PABCD 이어받는다지만 P/A/B/C/D가 읽고/해결/테스트할 의무 규칙 없음 | ✅기계 R-K |
| R4-4 | backfill 출력 경계 | R-G "유저 질문 X"가 좁음 — 약한 근거를 verified fact로 둔갑 가능 | ✅기계 R-L |
| R4-5 | flags.interview 주체 | R-I/R-H 하에서 auto-set이면 빈약 spec 진행 | ❓JUN(OPEN#6) |
| R4-6 | Contrarian 톤 | 톤이 severity 좌우 → auto-assume vs escalate 분기 | ❓JUN(OPEN#3) |
| R4-7 | elicitation fallback | flag off 시 단일 순서 정책 미정 | ❓JUN(OPEN#5) |
| R4-8 | self-cert 비독립성 | OPEN ASSUMPTIONS를 self-certifier가 작성 → 안전장치가 독립 아님 | ✅수용(R-I 신뢰기반, 한계 명시) |
| R4-9 | pressure-to-ask | backfill 모순이 유저 입력 필요한데 goal은 금지 → 미해결 결정이 build로 | ✅기계 R-L(defer 규칙) |

### dry-run #4 기계적 결정 (jun 승인 불요, R-E "핵심만 코드 + 행동규약" 일관)
- R-J [R4-1]: 해시 정책 — goal 시작 시 manifest.sha256를 현재 플랜파일과 **재계산 비교**. 불일치면
  "정본 변경됨" 경고 + 현재파일 기준 manifest 재freeze(자동) 후 진행. stale 실행 금지. (검증 시점=goal start.)
- R-K [R4-3]: PABCD가 OPEN ASSUMPTIONS 소비 의무 — A(audit)는 각 open assumption을 리스크로 검토,
  C(check)는 가능하면 테스트로 도전, D는 미해결 assumption을 결과에 명시. (행동규약 directive로.)
- R-L [R4-4/R4-9]: backfill 출력 = 3종만 — verified fact(코드/repo 근거 有) | contradiction | assumption.
  **추론을 fact로 둔갑 금지**(근거 없으면 assumption). 유저 입력이 유일 해법인 모순은 fact化 금지 →
  high-severity assumption으로 남기고 D 단계에서 "유저 확인 필요"로 defer(인터뷰는 goal 밖에서).
- R4-2 FIXED: 080 §1 interface/isInterviewReady/테이블/directive/테스트를 R-H 계약으로 정규화 완료.

### dry-run #4 product call (jun, 2026-06-30) → 080 RESOLVED #6
- R-M [R4-5/#6]: flags.interview = 유저 명시 트리거(omo식 description 키워드) OR 메인 자동(isInterviewReady).
  둘 다 허용. omo도 codex spawn_agent 실사용 확인 → D2 재확인.
- R-N [R4-7/#5]: elicitation fallback 불필요 — request_user_input은 플러그인 전제(항상 켜짐). OPEN #5 폐기.
- R-O [R4-6/#3]: Contrarian severity = 고정 규칙(high=아키/보안/요구모순/유저-only, medium=default가능, low=사소). 톤과 분리.
- ✅ 모든 ❓JUN 해소. 잔존 = 없음(다음 라운드에서 모순 0 확인 목표).
