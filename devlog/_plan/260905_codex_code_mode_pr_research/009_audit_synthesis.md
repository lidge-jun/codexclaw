# WP0 audit synthesis

Round 1 source range: `065fa1e8 → ff49029e`. Reviewers: Lorentz (`01a0705d-24c5-7793-b886-da46fa65ad08`) and Hilbert (`01a0705d-254f-79e3-9464-9b5167ee631d`). Actual verdicts: FAIL (three High) and GO-WITH-FIXES (two Medium). Main does not advance A→B on these results.

| Finding | Root cause | Main disposition and plan amendment |
| --- | --- | --- |
| Global cxc on inherited PATH | Candidate isolation covered files/home but not executable resolution | Accept. Generate experiment-owned cxc/codex launchers after validation, controlled PATH and pinned candidate dispatcher; record identities and exercise a hostile/conflicting global cxc fixture |
| Payload validation after doctor execution | Root validation did not validate contained dispatcher/dependency links before execution | Accept. Recursively validate/hash payload before doctor; record(spec) negative fixture proves a symlink target's marker is never written |
| Receipt then review document | Final document write changes source fingerprint used by C→D | Accept. Finalize all tracked evidence/review docs before the terminal receipt; recapture if anything changes afterward |
| Missing Interview rule owner | Hook reduction assumed done/better and trade-off duties already fully lived in the target skill | Accept. Explicitly relocate those pre-P duties into Interview before shrinking hook text; add a native Interview classification scenario |
| No implementation-worker delivery trial | Read-only explorer samples did not cover global delivery consumers | Accept. Add a paired disposable worker case with two allowed write paths, reference-defined behavior, independent oracle, required actual test execution and unchanged outside-scope sentinel |

No conflicts among the five amendments. Executable isolation and pre-execution validation reinforce the same candidate boundary; they do not add a production dispatcher. The Interview relocation retains existing semantics rather than adding a method. The worker case expands evidence, not product permissions.

Retained constraints: no hook-trust bypass, no global config mutation, no source or payload switching during a run, no OCX tier-echo repair, no weak phrase checks as behavior proof. Real source/model/high/priority evidence remains required; the known default echo is non-blocking by user instruction.

After the exact plan amendments, rerun document links and proposed-JS syntax checks, commit the interdiff, and send each original reviewer its blocker closure evidence. Any new blocker returns to this synthesis before another repair or dispatch.
