# wp8: release 0.2.39 and close #243

This phase ships #243 (the subagent dispatch card, spawn-ready role arguments and serialized MCP replies) as codexclaw 0.2.39. It reuses the train that published 0.2.38 hours earlier (050_release.md with its folds, and 051_delivery.md), with fresh evidence at every step.

Continuity: wp7 D closed at the "record wp7 delivery" commit with the dispatch card verified. origin/dev is 28f7921d. main is 92d26a60, and v0.2.38 is the latest release; no v0.2.39 tag or release exists (checked before dispatch).

## Version files

Same list as 050 (`rg --hidden -l '0\.2\.38' --glob '!.git' --glob '!CHANGELOG.md' --glob '!devlog/**' --glob '!node_modules/**'`): package.json, package-lock.json (every `"version": "0.2.38"`), cli/package.json, plugins/codexclaw/gui/package.json, plugins/codexclaw/components/*/package.json → 0.2.39. plugins/codexclaw/.codex-plugin/plugin.json → `0.2.39+codex.<UTC stamp>`. inventory.json and the three README badges are regenerated with `inventory.mjs --write --tests <measured total>`. durable-goalplan.md compatibility lines that name 0.2.37/0.2.38 stay.

## CHANGELOG 0.2.39

- Added: the SessionStart subagent dispatch card (the V1 override card, a one-cell resolver for an unresolved family, and dated model aliases checked against the local catalog); `subagents_get` `spawnArgs`, `staleModel` and `staleReason` (#243).
- Fixed: the subagent-config MCP stdio server answers in request order and finishes queued replies at EOF. The extended-length cwd test no longer pins JS `realpathSync` failing (#245; it landed after the 0.2.38 CHANGELOG was written).
- Compatibility: `subagents_get` can take up to 5 s longer while it probes the catalog; the response keeps its envelope and only adds fields.
- Verification: the measured test total and the host smoke test.

## Gates and delivery

Local: `npm run build`, `npm test` under `cxc receipt test`, `npm run gate`, `inventory.mjs --check --tests <total>`, `check-versions.mjs 0.2.39`, a leftover-version `rg` over the files check-versions skips, and `npm ls --workspaces --depth=0`. Privacy grep over the push range. Then an ordinary PR to dev (read the jobs at the exact head), squash-merge, dev exact-SHA CI, packed install and WSL; a dev → main PR "Release codexclaw 0.2.39" merged with a merge commit; main exact-SHA CI and packed install; release.yml dry run, then publish, with the full expected_sha; download the assets, `shasum -a 256 -c SHA256SUMS`, and compare the payload with `git archive <expected_sha> plugins/codexclaw`. Close #243 with a comment stating that the first-cell spawn replaces the "no ALL_TOOLS scan" wording and that V2 is proven only against a harness. Record the evidence in 071_delivery.md.

Flaky tests seen in 0.2.38 (the macOS hook-observation spawn timeout, and Windows Node image drift) are diagnosed from logs before any rerun. A rerun of failed jobs is used only for a confirmed timeout flake.
