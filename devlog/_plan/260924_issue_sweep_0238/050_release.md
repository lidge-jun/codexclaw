# wp6: release 0.2.38 and close the swept issues

This phase ships wp2-wp5 as codexclaw 0.2.38 and closes #240, #241, #239 and #232 with evidence. It follows the 0.2.37 train (devlog/_plan/260923_native_desktop_acceptance/030_release.md) with fresh evidence at every step; that file is a template, not proof that today's CI or release state is ready.

## Version files (resolved on fffdc923)

`rg -l '0\.2\.37' --glob '!CHANGELOG.md' --glob '!devlog/**'` returns exactly these files; each change is listed.

| Path:line | Before | After |
|---|---|---|
| package.json:3 | `"version": "0.2.37"` | `"version": "0.2.38"` |
| package-lock.json (13 lines matching `"version": "0.2.37"`: the root entry :3, :9 and every workspace package entry) | `0.2.37` | `0.2.38`; then `npm ls --workspaces` must report no invalid entries |
| cli/package.json, plugins/codexclaw/gui/package.json, plugins/codexclaw/components/{bg-wake,config-guard,cxc-ops,messenger-bridge,pabcd-state,provider-bridge,recall,skill-search,subagent-config}/package.json (line 3 each) | `"version": "0.2.37"` | `"version": "0.2.38"` |
| plugins/codexclaw/.codex-plugin/plugin.json:3 | `"version": "0.2.37+codex.20260923052558"` | `"version": "0.2.38+codex.<UTC yyyymmddHHMMSS of the release commit>"` |
| plugins/codexclaw/inventory.json:5-6, :306-354 | 0.2.37 manifest/package/component versions | regenerated by `node plugins/codexclaw/scripts/inventory.mjs --write --tests <total>` (without `--write` the script only checks, inventory.mjs:363-405); the new dev-devops oracle script also changes its script list |
| plugins/codexclaw/skills/loop/references/durable-goalplan.md:66 | "Builds older than 0.2.37 drop `desktop`" | unchanged: a compatibility fact about 0.2.37 |
| plugins/codexclaw/skills/dev-devops/references/native-desktop-acceptance.md:27, :31-32 | "codexclaw 0.2.37+", "needs 0.2.37 or newer" | unchanged for `desktop`; 040's SoT edit adds the 0.2.38 requirement for `--presented` next to it |
| README.md:16, README.ko.md:16, README.zh.md:16 | `tests-3%2C528` / `3,528 tests` | measured total from the final `npm test` |

## Documentation outside the skills

| Path:line | Change |
|---|---|
| docs-site/src/content/docs/reference/commands.md:131-140 | Add the mutating verbs with their flags (`add-criterion --session --criterion [--surface logic\|web\|tui\|desktop] [--presented native]`, `add-work-phase`, `add-task`, `complete-task`, `meet-criterion`, `steer`, `ready`) as the per-verb table in 030 defines them, and the sentence "Each verb accepts only its own flags; an unknown, misspelled or misplaced flag is an error and nothing is written." |
| docs-site/src/content/docs/guides/skills.md:102, :105 | unchanged: the cxc-qa and cxc-dev-devops descriptions still hold |
| CHANGELOG.md:7 | new `## [0.2.38] - <date>` under `## [Unreleased]` with Fixed (#240), Added (#241 locale lint and SVG crossing diagnostic in `report.notes`/P2, #239 artifact identity validation with criterion binding, `--presented native`, lipo oracle), Changed (goalplan CLI flag validation, contents column, DIAGRAM-LAYOUT-01), Compatibility (callers that passed ignored flags now fail; v2+ final gates need a 0.2.38 QA receipt for non-native desktop criteria; builds older than 0.2.38 drop `presented`), Verification (measured counts) |

The release doc reads no other docs-site page: `rg -n 'surface|artifact-identity|lipo|export-paged-report|DIAGRAM-LAYOUT|--presented' docs-site/src/content/docs` on fffdc923 shows no other page describing changed behavior.

## Local gates at the release commit

`npm run build`, `npm test`, `npm run gate`, `node plugins/codexclaw/scripts/inventory.mjs --check --tests <total>`, `node plugins/codexclaw/scripts/check-versions.mjs 0.2.38`. Each runs fresh; the C receipt wraps `npm test` through `cxc receipt test`. Before the first push, grep the push range for client or personal identifiers (DEV-PRIVACY-01); the unit contains synthetic fixtures only.

## Delivery

1. Push `codex/issue-sweep-0238` and open an ordinary PR to dev. Attach it to the task. Wait for every PR check and read jobs per DEV-CI-EVIDENCE-01 (expected jobs ran, head SHA, event, run id, attempt).
2. Squash-merge to dev as #237 was. Confirm dev exact-SHA CI, Packed install lifecycle and WSL.
3. Refresh open PRs and releases for collisions. Open the dev → main PR titled "Release codexclaw 0.2.38", attach it, merge with a merge commit as #238 was, and confirm main exact-SHA CI and Packed install lifecycle (release.yml requires them).
4. Dispatch release.yml with version 0.2.38, the full 40-character main SHA as expected_sha, dry_run=true; read the run's jobs. Then dispatch with dry_run=false. Read both runs.
5. Download the v0.2.38 assets, run `shasum -a 256 -c SHA256SUMS`, extract, and compare shipped files with the checkout's plugins/codexclaw subtree.
6. Close #240, #241, #239 with comments naming the fixing commit on main, the release, and what remains out of scope (#241: the SVG check is a REVIEW diagnostic; #239: the render observation stays an advisory, the identity gate applies to v2+ final gates, and the lipo oracle is behavioral on macOS only). Comment on #232 that its guidance shipped in 0.2.37 and its code follow-ups in #239 shipped in 0.2.38, then close it.
7. Record delivery evidence in 051_delivery.md.

Not in scope: installing 0.2.38 on any host, hook retrust, #209 and #213.

## Verifier notes

The release workflow's own `cxc release verify` fails closed on missing exact-SHA evidence (release.yml header comment). Hosted runs are the only evidence for Linux, Windows and WSL behavior; local macOS runs do not stand in for them.

## Audit round 1 folds

- Finding 11: every version file, README line, docs-site page and CHANGELOG anchor is resolved above with its before/after text.
- Audit round 2, finding 3: inventory regeneration uses `--write --tests <total>`; the `--check` gate runs after it.

## wp6 P revalidation (2026-09-24)

Continuity: wp5 D (dc27c660) closed the last implementation phase and named this release. The direction is unchanged.

Fresh state: origin/dev is still d66dfcf2 and this branch contains it (33 commits ahead). origin/main is 8b266161, the 0.2.37 promotion. The latest published release is v0.2.36. Release run 35826473652 for 8b266161 was a dry run (`DRY_RUN: true`), and no v0.2.37 tag or release exists, so 0.2.37 was never published. Following the 0.2.36 precedent (publish the next unused patch rather than backfill an unpublished one), this phase publishes 0.2.38. Its payload also carries the 0.2.37 changes, and the CHANGELOG keeps the existing 0.2.37 section. The release notes and the closing comments say that v0.2.38 is the first published release containing the 0.2.37 native desktop guidance.

The version file list above still holds (`rg -l '0\.2\.37'` returns the same files). durable-goalplan.md:66 and native-desktop-acceptance.md keep their compatibility statements. The measured test total is 3,599 at 43372970; the release commit remeasures it. The docs-site commands.md edit already landed in wp4.

Build: main does the version bump, CHANGELOG, inventory regeneration and badge. Check: build, full npm test under the receipt, gate, inventory --check, check-versions 0.2.38. Delivery follows steps 1-7 above.

## wp6 architect reflection folds

- #232 comment: its guidance landed on main in 0.2.37 and first ships in the published v0.2.38; the comment says exactly that.
- CHANGELOG: the 0.2.38 section opens with a line stating that it is the first published release containing the 0.2.37 changes (native desktop acceptance guidance, macOS approvals reference, `desktop` criterion surface) and points to the 0.2.37 section, because release.yml curates notes from the 0.2.38 section only (release.yml:287, .github/scripts/changelog-section.cjs:42).
- Version check: check-versions.mjs does not read cli/package.json, plugins/codexclaw/gui/package.json or package-lock.json. The local gate adds `rg -n '"version": "0\.2\.37"' package.json package-lock.json cli/package.json plugins/codexclaw/gui/package.json plugins/codexclaw/components/*/package.json`, which must print nothing, and `npm ls --workspaces --depth=0`, which must exit 0.
