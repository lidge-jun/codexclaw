# #243 architect consultation (wp7)

Architect 01a0d25e-dca9-7662-9f5b-799ee5555416 (gpt-6-sol). The discovery report and the full proposal are kept outside the checkout at .codexclaw/evidence/01a0d143-ac30-70c0-b494-27e596c0c7a7/exp243.md and arch243.md.

| ID | Proposal | Main disposition |
|---|---|---|
| E1 | A card with an unresolved family gives a one-cell snippet that picks the unique `spawn_agent` helper from `ALL_TOOLS`, chooses V1 or V2 arguments from its name, and calls it in the same cell; it stops on zero or several matches. Only a proven V1 signal prints `tools.multi_agent_v1__spawn_agent` directly; the V2 nested spelling is not printed as exact. | Accepted. Proven V1 signal = `CODEXCLAW_SPAWN_V1=1` (the existing override in capabilities.ts). The issue's literal "no ALL_TOOLS scan" becomes "spawn in the first exec cell" and is stated as such in the closing comment. |
| E2 | Extend the existing SessionStart hook (fallback-dispatch-cli session-start), emitting the card even without fallbacks; keep child suppression and fail-open; cap the combined context; no cross-event dedupe. | Accepted, with a 1,200-character cap for the card; the existing fallback protocol keeps priority. |
| E3 | Explicit dated alias map (deepseek, swe2, kimi, sol, luna), each alias kept only when the local Codex catalog contains its id, else shown as unverified with the map date. No `ocx models live` call at SessionStart. User-supplied full ids stay verbatim. | Accepted. The map lives in one source file with a `verifiedAt` date. |
| E4 | `subagents_get` decorated at the MCP boundary with `spawnArgs` per role and `staleModel` true/false/null; true only from a fresh authoritative catalog; the envelope is unchanged. | Accepted. "Fresh authoritative" = a successful `ocx models live` read in this call (not the last-success cache) when ocx is present, else the local Codex catalog counts as authoritative only when its file was modified within 24 h; otherwise null. |
| E5 | Doc lines in delegation.md under the V1 table and in native-execution.md; no new skill rule. | Accepted. |
| E6 | Tests for the hook (V1, unresolved, no fallback, child suppression, repeated invocation, budget), aliases (catalog absent, verified/unverified, full-id passthrough), MCP (spawnArgs, staleModel three states, envelope). A host smoke test of the V2 spelling is required before V2 is claimed. | Accepted. This host is V1; the V2 path stays "resolve by suffix" and no V2 spelling is claimed, so no V2 smoke test is needed. The V1 smoke test runs the card's snippet in a real Code Mode cell in this session. |
