# V1 architect consultation without repeated exception approval

V1 can already route a logical architect through its supported message/items input. The installed delegation instructions nevertheless demand a native `agent_type` field that this schema does not expose. This unit removes that contradictory setup requirement while preserving independent design consultation, audit and actual permission boundaries.

## Loop contract

Satisfy-spec; trigger: the reported repeated exception prompt and authorized repair through deployment. Goal: V1 callers proceed with real logical architect consultation; typed hosts retain native architect dispatch. Non-goals: new role configuration, registration, host schema changes, visualizer changes, permission weakening or new wrapper APIs. Verifiers: existing spawn-wrapper/hook tests (124 baseline passes), `node plugins/codexclaw/scripts/gate.mjs` (baseline exit 0), independent semantic audit of the changed instructions, dev PR CI and published payload inspection. The tests pass explicit target paths; gate.mjs traverses skill reference Markdown and declared structure documents for its existing drift rules, but neither proves the semantic wording correct. Stop only when the fix is merged and its release payload verified. Evidence: this unit plus local `.codexclaw/evidence/`. Outcomes: DONE with published proof; otherwise accurately report unmet requirements. Escalation: concrete new access/permission, failed guards or conflicting changes; routine authorized transport choices need no repeated question.

Scope: this checkout and lidge-jun/codexclaw PR delivery using existing credentials. User supplied no token/cost/time bound; platform limits still apply. C3 compatibility guidance repair, with C4 release verification performed by the designated integration owner. One work-phase, wp1, covers this bounded correction and release confirmation; no separate implementation stages.

## Delivery ownership

Ordinary PR `codex/v1-architect-compat` -> `dev`. This task owns implementation, review, CI and dev merge. The user-designated integration task owns version bump, dev->main promotion and release dispatch. No separate version reservation or release dispatch here. Final completion still requires independently checking the released payload. Rollback is a forward revert of the scoped instruction changes; prior immutable release assets remain available.

## Architect consultation

V1 architect handle: `01a0c804-f41b-7e00-8569-fd3377eceafe` (requested Sol, fresh context). Logical read-only packet with dev/dev-architecture attachments; no native permission claim. Proposal decisions D1/D3/D4/D6 accepted: schema-sensitive instructions, existing hook unchanged, synchronized SoT and preserved message/items skills. D2/D5/D7 amended: runtime builder expansion is deferred because current builders are explicitly V2-shaped helpers and a repository-wide call search found no production adapter consuming their output. The failing user path is the main agent's direct native V1 call. Its real successful architect dispatch and existing hook tests establish that new runtime behavior is not needed. Plan reflection and independent verdict will be recorded before B.

## Continuity

P: baseline dev d9d8a086; clean managed checkout adopted in place. See 001_research.md and 010_compatibility.md.

P reflection: the same architect returned ALIGNED with no remaining gaps after the no-agent_type message/items matrix clarification. D1-D7 dispositions unchanged. A subsequent comment-only clarification to architect.toml mirrors D1 and changes no design decision. Independent A audit follows; reflection does not authorize B by itself.
