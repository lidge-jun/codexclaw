---
title: codexclaw
description: "Codex-native development discipline: PABCD workflow, dev skills, subagent config, and an optional opencodex bridge, shipped as one Codex plugin."
template: splash
hero:
  image:
    file: ../../assets/codexclaw-hero.png
    alt: codexclaw split Codex blossom and OpenClaw claw logo
  tagline: "Development discipline for OpenAI Codex: one plugin, no fork, no harness."
  actions:
    - text: Get Started
      link: /codexclaw/getting-started/installation/
      icon: right-arrow
      variant: primary
    - text: How It Works
      link: /codexclaw/concepts/how-it-works/
      icon: open-book
    - text: GitHub
      link: https://github.com/lidge-jun/codexclaw
      icon: external
      variant: minimal
---

**codexclaw** (`cxc`) is a single [OpenAI Codex](https://openai.com/codex) plugin. It reuses the
Codex runtime and adds development discipline on top: a `cxc-dev` work classifier, an IPABCD
work loop driven by hooks and file state, multi-model subagent configuration over MCP, a shipped
GUI dashboard, and an optional detect-only [opencodex](https://github.com/lidge-jun/opencodex)
provider bridge.

It is not a Codex fork, not a harness, and not a provider proxy. Everything ships through the
standard Codex plugin surfaces: skills, hooks, an MCP server, local components, and a small `cxc`
CLI. The only scoped server exception is the opt-in loopback messenger bridge, which relays to
stock `codex exec` and does not orchestrate subagents.

<section class="cxc-band">

## PABCD pipeline

<div class="cxc-phase-strip" aria-label="codexclaw IPABCD pipeline">
  <span><strong>I</strong> Interview: settle ambiguity before a work loop starts.</span>
  <span><strong>P</strong> Plan: write the concrete change plan and evidence target.</span>
  <span><strong>A</strong> Audit: check risk, contradictions, and missing proof.</span>
  <span><strong>B</strong> Build: make the scoped change and verify as you go.</span>
  <span><strong>C</strong> Check: run the real build, tests, and review gate.</span>
  <span><strong>D</strong> Done: summarize evidence, close the phase, return to idle.</span>
</div>

```
Codex runtime
  └─ codexclaw plugin
       ├─ skills   →  cxc-dev discipline + dev-* routers + PABCD/loop/interview
       ├─ hooks    →  PABCD state, goal guards, provider detection
       ├─ MCP      →  subagent model/prompt config
       ├─ GUI      →  dashboard + opt-in messenger bridge
       └─ cxc CLI  →  enable · status · doctor · orchestrate · gui
```

</section>

<section class="cxc-band">

## Surface map

| Surface | Status | Count / scope | Primary docs |
|---------|--------|---------------|--------------|
| Skills | <span class="cxc-badge cxc-badge--current">current</span> | 25 skills: six implicit skills (`dev`, `search`, `interview`, `pabcd`, `recall`, `loop`), dev routers, `qa`, `repo-map`, and research/search helpers. | [Skills](/codexclaw/guides/skills/) |
| Hooks | <span class="cxc-badge cxc-badge--current">current</span> | 12 hook JSON files: `SessionStart` x2, `UserPromptSubmit` x1, `Stop` x1, `PreToolUse` x4, `PostToolUse` x2, `SubagentStop` x1, `PostCompact` x1. | [Hooks](/codexclaw/reference/hooks/) |
| MCP | <span class="cxc-badge cxc-badge--current">current</span> | Subagent model and prompt config: `subagents_get`, `subagents_set`, `catalog_list`. | [MCP Tools](/codexclaw/reference/api-mcp/) · [Subagents](/codexclaw/guides/subagents/) |
| Components | <span class="cxc-badge cxc-badge--current">current</span> | 8 component packages: config guard, ops, recall, PABCD state, provider bridge, subagent config, messenger bridge, skill search. GUI is separate. | [How It Works](/codexclaw/concepts/how-it-works/) |
| `cxc` CLI | <span class="cxc-badge cxc-badge--current">current</span> | `enable`, `disable`, `uninstall`, `status`, `orchestrate`, `freeze`, `metric`, `divergence`, `loop`, `goalplan`, `doctor`, `reset`, `subagents`, `map`, `provider`, `chat`, `memory`, `skill`, `gui`, `serve`, `service`. | [Commands](/codexclaw/reference/commands/) |
| GUI | <span class="cxc-badge cxc-badge--current">current</span> | Dashboard plus shipped opt-in loopback messenger bridge through `cxc serve`; not a jaw-style orchestrator. | [GUI Dashboard](/codexclaw/guides/gui/) |

</section>

<section class="cxc-band">

## Quick start

```bash
# From a source checkout
node bin/codexclaw.mjs enable

# Verify the install
cxc doctor

# Start a PABCD work-phase from the terminal
cxc orchestrate P --attest '{"from":"IDLE","to":"P","did":"plan the change"}'
```

The project gate is green at 644 tests. Build and test commands are documented separately so the
landing page stays a control surface, not a release note.

:::note[Status honesty]
These docs mark features `current`, `planned`, or `deferred`. A `planned` feature has a decided
design but no shipped runtime yet. The docs never describe a planned feature as if it shipped.
:::

</section>

<section class="cxc-band">

## Where to next

| Need | Go to |
|------|-------|
| Install or dogfood locally | [Installation](/codexclaw/getting-started/installation/) |
| Understand the plugin boundary | [How It Works](/codexclaw/concepts/how-it-works/) · [Plugin Boundary](/codexclaw/concepts/plugin-boundary/) |
| Run the IPABCD loop | [PABCD Workflow](/codexclaw/guides/pabcd/) |
| Inspect the skill model | [Skills](/codexclaw/guides/skills/) · [Work Classes](/codexclaw/concepts/work-classes/) |
| Configure subagents | [Subagents](/codexclaw/guides/subagents/) · [MCP Tools](/codexclaw/reference/api-mcp/) |
| Check shipped, planned, and deferred scope | [Parity Roadmap](/codexclaw/development/parity-roadmap/) |

</section>
